diff --git a/profiles/openscholar/modules/contrib/boxes/boxes.module b/profiles/openscholar/modules/contrib/boxes/boxes.module
index f660c55..8fadca4 100644
--- a/profiles/openscholar/modules/contrib/boxes/boxes.module
+++ b/profiles/openscholar/modules/contrib/boxes/boxes.module
@@ -182,6 +182,7 @@ function boxes_block_view($delta) {
     // Generate content and provide an editing form if user has proper
     // permissions.
     $block = $box->render();
+    if (!isset($block['delta'])) dpm($box);
     if (boxes_access_edit()) {
       if (variable_get('boxes_edit_location', BOXES_EDIT_IN_PLACE) == BOXES_EDIT_IN_PLACE) {
         $edit_link = array(
@@ -373,14 +374,19 @@ function boxes_box_load($delta = NULL) {
   if (isset($delta)) {
     $box = boxes_box::load($delta);
     drupal_alter('boxes_box_load', $box, $delta);
-    return $box;
+    if ($box instanceof boxes_box) {
+      return $box;
+    }
+    return false;
   }
   ctools_include('export');
   $boxes = array();
   foreach (ctools_export_load_object('box') as $box) {
     $box = boxes_box::load($box->delta);
-    drupal_alter('boxes_box_load', $box, $box->delta);
-    $boxes[$box->delta] = $box;
+    if ($box instanceof boxes_box) {
+      drupal_alter('boxes_box_load', $box, $box->delta);
+      $boxes[$box->delta] = $box;
+    }
   }
 
   if (module_exists('spaces') && $space = spaces_get_space()) {
@@ -392,8 +398,10 @@ function boxes_box_load($delta = NULL) {
       $box = boxes_factory($space_box->plugin_key, $space_box);
       $box->new = FALSE;
 
-      drupal_alter('boxes_load', $box, $box->delta);
-      $boxes[$box->delta] = $box;
+      if ($box instanceof boxes_box) {
+        drupal_alter('boxes_load', $box, $box->delta);
+        $boxes[$box->delta] = $box;
+      }
     }
   }
 
