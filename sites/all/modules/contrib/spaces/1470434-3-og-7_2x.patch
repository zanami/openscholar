diff --git a/sites/all/modules/contrib/spaces/init_frontpage.patch b/sites/all/modules/contrib/spaces/init_frontpage.patch
new file mode 100644
index 0000000..cbabe38
--- /dev/null
+++ b/sites/all/modules/contrib/spaces/init_frontpage.patch
@@ -0,0 +1,19 @@
+diff --git a/sites/all/modules/contrib/spaces/plugins/spaces_controller_variable.inc b/sites/all/modules/contrib/spaces/plugins/spaces_controller_variable.inc
+index d62d8b1..08d4b56 100644
+--- a/sites/all/modules/contrib/spaces/plugins/spaces_controller_variable.inc
++++ b/sites/all/modules/contrib/spaces/plugins/spaces_controller_variable.inc
+@@ -26,11 +26,9 @@ class spaces_controller_variable extends spaces_controller {
+ 
+     // If we have overridden 'site_frontpage', we need to do some extra
+     // work to reinit the drupal path.
+-    // TODO: Is purl_language_strip still needed?
+-    //if ($this->get('site_frontpage') !== $this->get('site_frontpage', 'original') && function_exists('purl_language_strip')) {
+-    //  $_GET['q'] = purl_language_strip($_REQUEST['q']);
+-    //  drupal_path_initialize();
+-    //}
++    if ($this->get('site_frontpage') !== $this->get('site_frontpage', 'original')) {
++      drupal_path_initialize();
++    }
+   }
+ 
+   /**
diff --git a/sites/all/modules/contrib/spaces/spaces_og/plugins/space_og.inc b/sites/all/modules/contrib/spaces/spaces_og/plugins/space_og.inc
index 1ba72ca..929ac4c 100644
--- a/sites/all/modules/contrib/spaces/spaces_og/plugins/space_og.inc
+++ b/sites/all/modules/contrib/spaces/spaces_og/plugins/space_og.inc
@@ -4,15 +4,17 @@
  * Organic groups integration for Spaces.
  */
 class space_og extends space_type_purl {
-  var $group = NULL;
+  public $group = NULL;
+  public $group_type;
 
   /**
    * Constructor.
    */
   function __construct($type, $id = NULL) {
+  	$this->group_type = 'node';
     parent::__construct($type, $id);
     // Extend spaces object with og entity.
-    $this->og = entity_load_single('node', $this->id);
+    $this->og = entity_load_single($this->group_type, $this->id);
   }
 
   /**
@@ -215,9 +217,11 @@ class space_og extends space_type_purl {
    *  User entity.
    */
   function og_is_group_admin($group, $account) {
-    $membership = og_get_membership('node' ,$group->nid, 'user', $account->uid);
+  	
+  	list($nid) = entity_extract_ids($this->group_type, $group);
+    $membership = og_get_membership('node' ,$nid, 'user', $account->uid);
     if (isset($membership->state) && $membership->state == OG_STATE_ACTIVE) {
-      return og_user_access('node',$group->internalIdentifier(), 'manage group space', $account);
+      return og_user_access('node',$nid, 'manage group space', $account);
     }
     return FALSE;
   }
diff --git a/sites/all/modules/contrib/spaces/spaces_og/spaces_og.module b/sites/all/modules/contrib/spaces/spaces_og/spaces_og.module
index 34ccdab..f8f8182 100644
--- a/sites/all/modules/contrib/spaces/spaces_og/spaces_og.module
+++ b/sites/all/modules/contrib/spaces/spaces_og/spaces_og.module
@@ -639,7 +639,7 @@ function spaces_og_og_context_handler() {
 
   $space = spaces_get_space();
   if (isset($space->type) && $space->type == 'og') {
-    $gids[] = $space->og->gid;
+    $gids[$space->group_type][] = $space->og->nid;
   }
 
   return $gids;
